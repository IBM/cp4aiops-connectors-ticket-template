apiVersion: v1
kind: Service
metadata:
  name: testtemplate
  annotations:
    connectors.aiops.ibm.com/cert-create-enabled: "true"
  labels:
    app: testtemplate
    instance: connector
spec:
  selector:
    app: testtemplate
    instance: connector
  ports:
    - protocol: TCP
      port: 443
      name: https
      targetPort: 9443
